<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="zrock.application.engine"
	android:versionCode="58"
	android:versionName="3.6">

	<uses-sdk
		android:minSdkVersion="17"
		android:targetSdkVersion="25"/>

	<uses-permission
		android:name="android.permission.INTERNET"/>

	<uses-permission
		android:name="android.permission.ACCESS_NETWORK_STATE"/>

	<application
		android:allowBackup="true"
		android:icon="@drawable/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:resizeableActivity="true">

		<activity
			android:theme="@style/AboutLibrariesTheme"
			android:name="zrock.application.engine.EngineActivity"
			android:label="@string/app_name">

			<intent-filter>

				<action
					android:name="android.intent.action.MAIN"/>

				<category
					android:name="android.intent.category.LAUNCHER"/>

			</intent-filter>

		</activity>

		<activity
			android:name="zrock.application.engine.app.libraries.LibrariesActivity"
			android:label="@string/app_name"
			android:theme="@style/AboutLibrariesSampleTheme">

			<intent-filter>

				<action
					android:name="android.intent.action.MAIN"/>

				<category
					android:name="android.intent.category.CUSTUM_INTENT"/>

			</intent-filter>

		</activity>

		<activity
			android:theme="@style/AboutLibrariesTheme"
			android:name="zrock.application.engine.app.libraries.ExtendActivity"
			android:label="@string/app_name">

		</activity>

		<activity
			android:theme="@style/AboutLibrariesTheme"
			android:name="zrock.application.engine.app.libraries.CustomSortActivity"
			android:label="@string/app_name">

		</activity>

		<activity
			android:name="zrock.application.engine.app.libraries.LibsActivity"/>

		<activity
			android:name="zrock.application.engine.app.setting.SettingsActivity"
			android:label="@string/app_name"
			android:theme="@style/AboutLibrariesTheme"/>

	</application>

</manifest>

